{
  "name": "protifolio2-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14 <19"
  },
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "install-client": "cd client && npm ci"
  }
}
